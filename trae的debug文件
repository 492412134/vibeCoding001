# 规则引擎功能模块使用说明

## 功能概述
本规则引擎是一个轻量级的业务规则管理系统，支持通过JavaScript表达式定义规则条件和动作，使用内存集合存储规则配置（系统重启后规则会失效）。

## 技术实现

### 核心组件
1. **Rule 接口**：定义规则的基本方法（id、name、evaluate、execute、priority）
2. **BasicRule 类**：实现Rule接口，包含规则的基本属性和方法
3. **RuleEvaluator 类**：使用JavaScript引擎评估规则条件和执行动作
4. **RuleEngine 类**：管理规则的添加、删除、更新和执行（单例模式）
5. **RuleController 类**：提供REST API接口

### API接口
- `POST /api/rule/add` - 添加规则（自动生成规则ID）
- `DELETE /api/rule/remove/{id}` - 删除规则
- `PUT /api/rule/update` - 更新规则
- `GET /api/rule/list` - 获取规则列表
- `POST /api/rule/execute` - 执行规则

### 前端页面
- **首页**：http://localhost:10991/ - 包含规则引擎管理入口
- **规则引擎管理**：http://localhost:10991/rule-engine.html - 完整的规则管理界面

## 使用步骤

### 1. 访问规则引擎管理页面
1. 打开浏览器，访问 http://localhost:10991/
2. 点击"规则引擎管理"按钮进入管理页面

### 2. 添加规则
1. 在"添加规则"部分填写以下信息：
   - **规则名称**：规则的描述性名称
   - **条件**：JavaScript表达式，返回布尔值（例如：`age > 18 && score >= 90`）
   - **动作**：JavaScript代码，执行规则动作（例如：`result = '优秀';`）
   - **优先级**：规则执行的优先级（数字越大优先级越高）
2. 点击"添加规则"按钮
3. 系统会自动生成规则ID并提示添加成功

### 3. 查看规则列表
- 在"规则列表"部分可以看到所有已添加的规则
- 每条规则显示名称、ID、条件、动作和优先级
- 可以点击"删除"按钮删除规则

### 4. 执行规则
1. 在"执行规则"部分的"事实数据"文本框中输入JSON格式的数据（例如：`{"age": 20, "score": 95}`）
2. 点击"执行规则"按钮
3. 执行结果会显示在下方的"执行结果"区域

## 规则示例

### 示例1：年龄判断
- **规则名称**：成年人判断
- **条件**：`age >= 18`
- **动作**：`result = '成年人';`
- **优先级**：1
- **测试数据**：`{"age": 20}`
- **执行结果**：`{"age": 20, "result": "成年人"}`

### 示例2：分数评级
- **规则名称**：优秀评级
- **条件**：`score >= 90`
- **动作**：`grade = '优秀';`
- **优先级**：2
- **规则名称**：良好评级
- **条件**：`score >= 80 && score < 90`
- **动作**：`grade = '良好';`
- **优先级**：1
- **测试数据**：`{"score": 85}`
- **执行结果**：`{"score": 85, "grade": "良好"}`

## 技术特点
1. **轻量级**：使用内存集合存储规则，无需外部数据库
2. **灵活**：支持通过JavaScript表达式和代码定义规则
3. **可扩展**：模块化设计，易于添加新功能
4. **实时**：规则添加后立即生效，无需重启服务
5. **优先级**：支持规则优先级排序，高优先级规则先执行

## 注意事项
1. 规则配置存储在内存中，系统重启后规则会失效
2. 条件和动作使用JavaScript语法，需确保语法正确
3. 执行规则时，事实数据必须是有效的JSON格式
4. 规则执行过程中修改的变量会反映在返回结果中
