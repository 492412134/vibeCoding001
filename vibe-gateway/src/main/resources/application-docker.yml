# Docker 环境配置
# 覆盖默认的 application.yml 配置

spring:
  cloud:
    nacos:
      discovery:
        server-addr: host.docker.internal:8848
        namespace: public
      config:
        server-addr: host.docker.internal:8848

server:
  port: 8081
